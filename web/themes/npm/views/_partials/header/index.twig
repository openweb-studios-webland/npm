<header
  class="header {{ front_page ? 'bg-blue text-white ' : 'bg-white ' }}relative -mb-1 py-10 top-0 z-30 lg:py-30 lg:sticky"
  data-module="header"
  data-label="header"
>
  <div class="flex justify-between items-center">
    <div class="flex-0 px-20">
      <a href="/">
        {{ svg('logo', { classes: 'logo' }) }}
      </a>
    </div>

    {% if menu.primary.items | length %}
      {% set uid = random() %}

      <nav
        id="{{ uid }}"
        class="menu absolute flex-1 left-0 px-20 shadow-xs top-full w-screen z-40 lg:shadow-none lg:static"
        role="navigation"
        aria-hidden="true"
      >
        {% include '_partials/header/menu.twig' ignore missing with {
          uid: uid,
          items: menu.primary.items,
        } %}
      </nav>
    {% endif %}

    <div class="hidden flex-0 px-20 lg:block">
      <a class="{{ front_page ? ' button-outline-blue ' }}button-small" href="/contact">
        Contact Us
      </a>
    </div>

    <div class="px-20 lg:hidden">
      <button
        class="header-button h-44 relative -mr-10 w-44"
        data-module="toggle"
        aria-controls="{{ uid }}"
      >
        <span class="sr-only">
          Menu
        </span>
        {{ svg('menu', { classes: 'absolute inset-1/2 translate-1/2' }) }}
        {{ svg('menu-close', { classes: 'absolute inset-1/2 translate-1/2' }) }}
      </button>
    </div>
  </div>
</header>
