{% extends '_layouts/base.twig' %}

{% block content %}
  <div class="relative">
    {% if fields.blocks %}
      {% for block in fields.blocks %}
        {% include '_blocks/' ~ block.acf_fc_layout ~ '/index.twig' ignore missing with {
          block: block
        } %}
      {% endfor %}
    {% endif %}

    {{ svg('pattern', { classes: 'hidden max-w-full w-full lg:block' }) }}
  </div>
{% endblock %}
